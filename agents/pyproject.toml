[project]
name = "shifahealth-agents"
version = "0.1.0"
description = "ShifaHealth AI standalone ADK agent service"
requires-python = ">=3.11"

[tool.pytest.ini_options]
pythonpath = ["src"]

[tool.ruff]
line-length = 120
src = ["src", "tests"]

[tool.ruff.lint]
select = ["E", "F", "I", "UP"]
